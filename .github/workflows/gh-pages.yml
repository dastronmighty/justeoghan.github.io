name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Replace 'main' with your branch if different

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Allow actions to push to the repo

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20" 

      - name: Confirm Node.js version
        run: node -v  # This checks the installed Node.js version

      - name: Install react-scripts@latest
        run: npm install react-scripts@latest --save  # Install the latest react-scripts

      - name: Install dependencies
        run: npm install --legacy-peer-deps  # Use --legacy-peer-deps to resolve peer dependency conflicts

      - name: Build the project
        run: npm run build

      - name: Copy CNAME file
        run: |
          if [ -f CNAME ]; then
            cp CNAME dist/
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist  # Deploy content from the 'dist' folder
          publish_branch: gh-pages
          force_orphan: true  # Force orphan commit, no branch history
